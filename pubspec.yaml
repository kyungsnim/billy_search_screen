name: billy_search_screen
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^5.0.0
  retrofit: ^2.0.0
  dio: ^4.0.0
  substring_highlight: ^1.0.33    # 글자에 하이라이트 넣기
  intl: ^0.17.0                   # 숫자 단위 넣기
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: ^2.0.6
  retrofit_generator: ^2.0.0+3
  json_serializable: ^5.0.2

flutter:
  uses-material-design: true